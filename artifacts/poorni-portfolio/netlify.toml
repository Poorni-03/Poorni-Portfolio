[build]
  command = "pnpm --filter poorni-portfolio run build"
  publish = "dist/public"

[build.environment]
  NODE_ENV = "production"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
