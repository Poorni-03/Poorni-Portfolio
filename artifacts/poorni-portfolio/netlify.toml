[build]
  command = "pnpm install && pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_ENV = "production"
  BASE_PATH = "/"
  PORT = "4173"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
